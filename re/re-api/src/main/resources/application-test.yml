spring:
  # 配置数据源
  datasource:
    url: jdbc:mysql://localhost:3306/re?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: re#mysql2020
  # 配置redis
  redis:
    port: 6379
    host: 127.0.0.1
    password: re#redis2020

# 本项目相关配置
re:
  security:
    # 配置jwt过期时间 单位 小时
    token-expire: 12
    # 配置jwt秘钥
    token-secret-key: ROOTELEMENT
    # url白名单
    white-url:
      - /api/v1/system/user/login
      - /api/v1/system/verifyCode
      - /message/**
      - /v2/api-docs
      - /static/**
  swagger-ui-open: true
  static-file-base-path: /re/static

logging:
  level:
    root: info
    cn.ljtnono.re.api: debug