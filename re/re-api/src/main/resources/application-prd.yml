spring:
  # 配置数据源
  datasource:
    url: jdbc:mysql://localhost:8002/re?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: re#mysql2020
  # 配置redis
  redis:
    lettuce:
      pool:
        max-active: 8
        max-wait: 10000
        max-idle: 8
        min-idle: 0
      shutdown-timeout: 100
    port: 8003
    host: 127.0.0.1
    password: re#redis2020