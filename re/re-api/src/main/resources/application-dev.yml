spring:
  # 配置数据源
  datasource:
    url: jdbc:mysql://localhost:3306/re?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: you+me=love

  # 配置redis
  redis:
    lettuce:
      pool:
        max-active: 8
        max-wait: 10000
        max-idle: 8
        min-idle: 0
      shutdown-timeout: 100
    port: 6379
    host: 139.9.73.191
    password: ljtLJT715336
    timeout: 12000

logging:
  level:
    root: info
    cn.ljtnono.re.api: info
# 配置logging等级